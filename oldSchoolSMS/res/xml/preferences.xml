<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

	<PreferenceCategory android:title="View">

		<CheckBoxPreference android:key="showMessageContentInList"
			android:title="Show message" android:summary="Show message content in SMS list"
			android:defaultValue="true" />

		<EditTextPreference android:key="pageSize"
			android:title="Page size"
			android:summary="Maximum number of SMS messages in the main screen list"
			android:dialogTitle="pageSize" android:dialogMessage="pageSize"
			android:defaultValue="50" android:numeric="integer" />

	</PreferenceCategory>

	<PreferenceCategory android:title="Behaviour">
		
		<CheckBoxPreference android:key="notifyOnNewSms"
			android:title="Notify on new SMS" android:summary="Notify if new SMS arrives"
			android:defaultValue="true" />
		<CheckBoxPreference android:key="popupOnNewSms"
			android:title="Popup on new SMS" android:summary="Display popup window if new SMS arrives"
			android:defaultValue="true" android:dependency="notifyOnNewSms" />

	</PreferenceCategory>

	<PreferenceCategory android:title="Application developement">
		
		<CheckBoxPreference android:key="debugModeSmsList"
			android:title="SMS list" android:summary="Show debug informations from SMS list"
			android:defaultValue="false" />
		<CheckBoxPreference android:key="extendedBoxList"
			android:title="SMS box list" android:summary="Show extended list of SMS boxes"
			android:defaultValue="false" />

	</PreferenceCategory>

</PreferenceScreen>
